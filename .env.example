# Copy to .env and adjust for your environment. Do NOT commit .env.
# The docker-compose.yml falls back to "shortener" for local dev if these are unset.
POSTGRES_PASSWORD=shortener
DATABASE_PASSWORD=shortener
